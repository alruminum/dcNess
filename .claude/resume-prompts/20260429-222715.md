[이전 세션 이어가기 — DCN smart-compact 2026-04-29]

# 프로젝트
~/project/dcNess (main branch, plugin 개발 중). RWHarness fork-and-refactor — prose-only + Task tool 패턴.

# 최근 진행 (PR / Task-ID, 신→구)
- PR #37 (DCN-CHG-38): `/smart-compact` skill — 컨텍스트 압축 + resume prompt 생성
- PR #36 (DCN-CHG-37): `/quick` + `/product-plan` skills
- PR #35 (DCN-CHG-36): `/qa` skill (첫 사용자 진입점)
- PR #34 (DCN-CHG-35): multi-session e2e smoke (11 케이스)
- PR #33 (DCN-CHG-34): hooks/ 인프라 (session-start.sh + catastrophic-gate.sh + hooks.json)
- PR #32 (DCN-CHG-33): session_state.py 확장 (by-pid 레지스트리 + CLI 5 subcommand)
- PR #31 (DCN-CHG-32): docs/conveyor-design.md rewrite (Task tool 패턴)
- PR #30 (DCN-CHG-30): harness/session_state.py 신규 (멀티세션 격리 API)
- PR #29 (DCN-CHG-29): docs/conveyor-design.md 초안 (Python 컨베이어 — 폐기됨)
- PR #28 (DCN-CHG-28): orchestration_agent.py JSON 결정자 — **CLOSED, 미머지** (proposal §2.5 위반)

# 핵심 디자인 결정 (rationale)
1. **Task tool + Agent + helper + 훅 패턴 채택** (Python `run_conveyor` 폐기)
   - Python 안에서 Agent 호출 시 PreToolUse 훅 미발화 → catastrophic 보호 깨짐
   - Python batch 호출은 사용자 가시성 0 + 메인 자율도 0
2. **heuristic-only enum 추출** (haiku 미사용)
   - API 키 의존 회피, dcNess 자체 도그푸딩 호환
   - AMBIGUOUS 시 cascade (agent 재호출 → 사용자 위임)
3. **by-pid 레지스트리로 멀티세션 격리** (env `DCNESS_RUN_ID` 전파 폐기 — Bash 휘발성)
   - `.by-pid/{cc_pid}` ← sid, `.by-pid-current-run/{cc_pid}` ← rid
   - 글로벌 `~/.claude` 폴백 미채택
4. **catastrophic backbone via PreToolUse 훅** (코드 hardcode 0)
   - HARNESS_ONLY_AGENTS (engineer / validator-PLAN/CODE/BUGFIX_VALIDATION) + §2.3 4룰
   - 메인이 protocol 따르든 안 따르든 자동 발화

# 진행 중 작업
- branch: main (clean, .claude/scheduled_tasks.lock 만 untracked)
- 160/160 tests PASS
- skills 4개: /qa, /quick, /product-plan, /smart-compact

# 미해결 의논거리 (사용자 결정 대기)
1. **worktree 도입** — 트레이드오프 4 옵션 검토 완료, 옵션 C (keyword 트리거) 추천했으나 결정 보류
   - 옵션 A: 매번 자동
   - 옵션 B: 사용자 1회 확인
   - 옵션 C: 명시 keyword 시만 ← 추천
   - 옵션 D: 디폴트 + opt-out
   - **미해결 디테일**: sid fallback (worktree 진입 시 main repo `.claude/.by-pid/` 어떻게 cross-reference)

# 다음 단계 후보
- `/init-dcness` skill (enable/disable 부트스트랩) — 사용자 우선순위 표시
- `/ux` skill (designer + critic, Pencil MCP 의존) — 후순위
- worktree 도입 (위 미해결 의논 결정 후)
- governance §2.2 에 `commands/` 카테고리 추가 (현재 null 분류)
- manual smoke (실 `claude` 띄워 /qa /quick /product-plan 발화 검증)

# 폐기 결정 (이유 보존 — 재논의 회피)
- ❌ `/harness-monitor` `/harness-list` `/harness-kill` — Task tool 이 자연 가시화 → 불필요
- ❌ 옵션 (a) RWH `impl_loop.py` fork — 강결합 코드 흡수 부담
- ❌ 옵션 (b) 정적 dict 라우팅 — 분기 룰 hardcode 약점 (§2.5 원칙 1 위반)
- ❌ 옵션 (c) JSON 결정자 LLM (PR #28) — prose-only 원칙 충돌
- ❌ 단일 라벨 결정 하이쿠 (`ADVANCE/RETRY/INSERT_*`) — 라벨 늘면 hardcode 부담
- ❌ 관문 하이쿠 별도 LLM — 해석 하이쿠 + advance_when 비교로 충분
- ❌ 글로벌 `~/.claude/.session-id` 폴백 — 4-가드 복잡도 vs 도그푸딩 가치 ↓
- ❌ env `DCNESS_RUN_ID` 전파 — Bash subprocess 휘발성

# 컨텍스트 (자동 디스커버)
- 핵심 코드: `harness/session_state.py`, `harness/hooks.py`, `harness/signal_io.py`, `harness/interpret_strategy.py`, `harness/llm_interpreter.py`
- 훅: `hooks/session-start.sh`, `hooks/catastrophic-gate.sh`, `hooks/hooks.json`
- skills: `commands/qa.md`, `commands/quick.md`, `commands/product-plan.md`, `commands/smart-compact.md`
- spec: `docs/conveyor-design.md` (v2 Task tool 패턴, 660+ 줄), `docs/orchestration.md` (룰 SSOT)
- proposal: `docs/status-json-mutate-pattern.md` §2.5 (prose-only 대 원칙)
- governance: `docs/process/governance.md`, `docs/process/document_update_record.md`, `docs/process/change_rationale_history.md`
- agents: `agents/*.md` 13개 모두 prose-only 변환 완료 (DCN-CHG-27)

# 거버넌스 메모
- Task-ID 형식: `DCN-CHG-YYYYMMDD-NN`
- 매 PR 전: `node scripts/check_document_sync.mjs` PASS 필수
- main 직접 push 금지, branch → PR → squash merge
- `commands/` 변경은 docs-only 카테고리 (governance §2.2 미등록 — null 분류 검토 follow-up)

# 다음 세션 시작 가이드
1. PROGRESS.md 상단 5 entries 읽기 (사후 보강 entries 포함)
2. `docs/process/document_update_record.md` 의 -38 entry 읽기
3. 위 "미해결 의논거리" + "다음 단계 후보" 보고 사용자 의도 받기
4. (worktree 결정 시) `docs/conveyor-design.md` §4.6 PID 재사용 보호 + sid fallback 설계 의논 시작
