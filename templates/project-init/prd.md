# PRD: {프로젝트명}

> 기획 논의 후 채워넣을 placeholder. `{...}` 자리를 합의된 내용으로 교체한다.

## 목표

{이 프로젝트가 해결하려는 문제를 한 줄로 요약}

## 사용자

{누가 이 제품을 쓰는지 — 페르소나 / 역할 / 사용 맥락}

## 핵심 기능

1. {기능 1}
2. {기능 2}
3. {기능 3}

## MVP 제외 사항

> 의도적으로 *안 만들* 것 — 범위 팽창 방지

- {안 만들 것 1}
- {안 만들 것 2}
- {안 만들 것 3}

## 수용 기준

> 핵심 기능마다 "끝났다" 판정 기준 1줄. 모호함 줄이는 용도.
> **각 항목에 안정 ID `AC-NNN` 부여** (001부터 PRD 전역 순번, 한번 부여하면 불변). 이 AC-ID 가 origin — 이후 impl 의 `REQ-NNN` 이 `(from AC-NNN)` 으로 이 ID 를 인용해 PRD↔impl 추적성을 확보한다.

- **AC-001** (기능 1) — {예: 사용자가 X 입력 시 Y 결과가 1초 내 표시된다}
- **AC-002** (기능 2) — {...}
- **AC-003** (기능 3) — {...}

> **리터럴 규약도 AC 로 명시** — 경로 문자열·디렉토리 이름·파일 포맷처럼 *값 자체가 약속*인 것은 별도 AC 항목으로 박는다. impl 이 self-consistent 하게 틀린 값을 쓰는 것을 막는다 (PRD 가 origin).

- **AC-010** (저장 경로 규약) — {예: 버전 스크립트는 `script/v001_script.md` 형식, 이미지 history 는 `images_history/v001/` 하위}

## 디자인 방향

- {디자인 톤 (예: 다크모드 고정, 미니멀)}
- {색상 (예: 무채색 + 포인트 1가지)}
- 상세 시각 시스템: [`docs/design.md`](design.md) 참조
